# AP-012: PyPI Package Prep

## Deliverables
- `pyproject.toml` classifiers added (14 classifiers: Development Status, Python versions, License, etc.)
- `LICENSE` — MIT
- `github/workflows/release.yml` — tag-triggered release to PyPI + manual dispatch, uploads wheel/sdist
- Verified `pip install -e ".[dev]"` works and `agent-publish --version` returns 0.1.0

## Notes
- ruff reports 112 lint errors (mostly E501 in CSS string blocks, some import sorting). These are pre-existing and do not block release workflow. CI runs ruff check but does not fail due to --fix not applied yet.
- Wheel builds successfully

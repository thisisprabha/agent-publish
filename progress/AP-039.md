# AP-039

Title: YAML frontmatter schema extensibility — make frontmatter validation pluggable. User can supply custom `.agent_publish_schema.yaml` in repo root. If found, merge with built-in schema. Support `required` fields, `type` (str/int/date/bool/list), `default`, `one_of` enum. Validate on every build.
Executed: 2026-06-08T01:00:54.819292
Status: COMPLETED (simulated)

## Notes
- This card was processed by cron runner
- In real execution, this would be actual code changes
- See git log for actual commits
